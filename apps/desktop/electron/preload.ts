// IPC bridge, exposed via contextBridge once the renderer needs main-process
// access. Empty for now — this skeleton only needs a plain HTTP fetch to the
// backend, no privileged Electron/Node APIs.
